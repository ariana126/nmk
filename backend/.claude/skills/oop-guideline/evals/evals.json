{
  "skill_name": "oop-guideline",
  "evals": [
    {
      "id": 1,
      "prompt": "Reviewing a PR and this constructor is bugging me but I can't articulate why:\n\nconstructor(private moduleRef: ModuleRef, private config: AppConfig, private logger?: Logger) {\n  this.cacheDir = this.config.get('cache.dir')\n  if (!fs.existsSync(this.cacheDir)) fs.mkdirSync(this.cacheDir, { recursive: true })\n  this.userRepo = this.moduleRef.get(UserRepository)\n}\n\nIs this actually a problem or am I being precious about it?",
      "expected_output": "Should identify four distinct problems rather than a vague 'it's messy': (1) ModuleRef is a service locator — inject UserRepository directly, since the class retrieves the real dependency from it rather than using it; (2) AppConfig hides which values are actually used — inject `cacheDir: string` specifically; (3) the optional logger should be required with a NullLogger, otherwise `if (this.logger)` guards spread through every method; (4) mkdirSync is work in a constructor — merely instantiating leaves side effects, so push setup outward to a factory or bootstrap. Should mention that a constructor does exactly two things, validate and assign, and ideally cite the reordering test. Should NOT just say 'use dependency injection' without distinguishing inject-what-you-need from inject-where-you-get-it-from."
    },
    {
      "id": 2,
      "prompt": "our Order entity has like 30 getters and setters and the OrderService is 400 lines of stuff like `if (order.getStatus() === 'PENDING' && order.getPaymentDate() !== null) { order.setStatus('PAID') }`. everyone agrees it sucks but nobody knows where to start. where do we start?",
      "expected_output": "Should name this as an anemic domain model where invariants have no home, so every caller re-implements the rules and one of them is wrong. Should propose moving the decisions inside: a query-then-command pair on the same object is a conversation that belongs within it, so `order.markAsPaid()` replaces the getStatus/setStatus dance. Should recommend replacing setters with command methods and putting essential data through the constructor / named constructors (`Order.place()`), so the object is complete and valid the moment it exists. Should suggest watching what callers do with each getter as the way to prioritise — the tell that it worked is being able to delete the getter, not wrap it. Bonus: status as a value object or state transitions guarded by the entity, recorded domain events instead of getters for tests, and read models for the clients that only read."
    },
    {
      "id": 3,
      "prompt": "I need our NotificationSender to support Slack in addition to email. Plan was to make NotificationSender abstract with a protected send() and have EmailNotificationSender and SlackNotificationSender extend it — the retry logic and rate limiting are shared so I'd keep those in the base class. Sound reasonable?",
      "expected_output": "Should push back on inheritance without being dogmatic about it, explaining the actual cost: the subclass depends on the parent's protected internals, so renaming the hook or adding a parameter silently breaks every subclass. Should recognise this specific plan as the template method pattern — better than raw subclassing, but strictly weaker than composition — and give the one-step mechanical conversion: promote the abstract protected send() to a public method on an injected `NotificationChannel` interface, then mark the class final. Should place the shared retry/rate-limiting as decorators over that interface (RetryingChannel, RateLimitedChannel) rather than in a base class, noting that decoration keeps the cross-cutting logic in one place, agnostic to which channel is wrapped, and independently testable. Should climb the ladder explicitly rather than jumping straight to decorators."
    },
    {
      "id": 4,
      "prompt": "quick api design question — for our ShoppingCart class, is `addItem(item): number` returning the new item count fine? saves the caller a round trip",
      "expected_output": "Should identify the CQS violation: addItem both changes state and returns information, so the cart changes even when a caller only wanted to look, and a query is no longer safe to call any number of times. Should offer the two fixes — split into a void command plus an `itemCount()` query, or make the cart immutable so `withItem(item): ShoppingCart` has no state change left to separate. Should note the naming signal (imperative name with a non-void return is a confused signature) and that dropping the `get` prefix on the query matters. Should be proportionate to a 'quick question' — this is a small fix, not a redesign. Ideally acknowledges that CQS has legitimate exceptions (nextIdentity, repositories, controllers) but that saving a round trip isn't one of them."
    },
    {
      "id": 5,
      "prompt": "Here's a method signature from our billing code:\n\nfunction createSubscription(customerId: string, planId: string, amountCents: number, currency: string, trialDays: number, startDate: string): Subscription\n\nWe validate all of these at the top of the function — the currency has to be one of 5 supported ones, amount can't be negative, trialDays is 0-90, startDate has to parse and be in the future. It's about 25 lines of validation before anything happens. Same checks show up in updateSubscription and in the API controller too. What's the cleanest way to handle this?",
      "expected_output": "Should recommend extracting value objects rather than factoring the validation into a shared helper: the guiding question is 'would any string/int be acceptable here?' and for every one of these the answer is no. Should identify amountCents + currency as a composite value that always travels together → `Money`, which also removes the ambiguity in return types. Should cover CustomerId, PlanId, Currency, TrialPeriod, StartDate similarly. The key insight to hit: the precondition checks don't move, they *disappear* — wherever the type appears, validity is already guaranteed and the compiler enforces it, so the duplication across createSubscription/updateSubscription/controller is eliminated rather than centralised. Should mention naming for the concept not validity (`EmailAddress` not `ValidEmailAddress`) and named constructors (`Money.fromCents`, `StartDate.fromString`) with a private constructor. May note value objects act as attractors for behavior. Should NOT primarily recommend a validation library or a shared validate() function."
    },
    {
      "id": 6,
      "prompt": "We've got a GET /api/reports/inventory endpoint that's timing out. The controller does `productRepository.findAll()` then loops over every Product entity summing stock movements. ~180k products now. My instinct is to add caching in front of it but the numbers change constantly. Thoughts?",
      "expected_output": "Should diagnose two problems at once and say so: a performance problem (loading every entity ever created to answer one question) and a design problem (a read-only client holding write models — the endpoint can call anything on those entities, and if it starts modifying them one day, tracing what happened is hard). Should recommend a read model shaped for this use case with its own repository, built directly from the data source in one aggregating SQL query — the default because it's cheapest at runtime AND in maintenance. Should note the controller then never touches the write model, and the success signals: the Product entity's getters can be deleted, and the client stops transforming what it receives. Should explicitly not jump to event-sourced/projected read models, which are the expensive option justified only when the write model changes often, raw data needs interpretation, or recomputation is genuinely too costly — and should note event sourcing is a separate, larger commitment that isn't required here. Should treat caching as addressing the symptom."
    },
    {
      "id": 7,
      "prompt": "Working through a legacy Django codebase and I want to apply proper object design. Should I make every model a value object with a private constructor and named constructors? The ORM instantiates these itself so I'm not sure how far to push it.",
      "expected_output": "Should push back on applying the rules mechanically and reason about the actual constraint rather than reciting the guideline. Key points: Django models are entities, not value objects — they have identity, a life cycle, and are persisted by and retrieved from a repository-equivalent, so mutability is correct for them and the value-object ruleset doesn't apply. Should acknowledge that an active-record ORM that constructs objects itself genuinely fights private constructors and named constructors, and that Noback's own caveat covers this: bend the rules when the effort outweighs the benefit. Should offer what IS worth doing in this context — command methods instead of setter-then-save, extracting value objects for the fields where any string wouldn't be acceptable, moving decisions off callers and into the model, read models for read-only views — while being honest that full construction control is where the ORM wins. Should NOT recommend a wholesale rewrite, and should NOT claim the rules apply unchanged."
    },
    {
      "id": 8,
      "prompt": "Our ShippingCostCalculator has a switch on carrier that's up to 6 branches now (ups, fedex, dhl, royal_mail, local_courier, pickup) and each branch is 20-40 lines. Adding a 7th next sprint. The obvious move is to subclass the calculator per carrier, right?",
      "expected_output": "Should name the axis of variation before naming a pattern — what varies here is the algorithm, so this is Strategy — and should explicitly reject the subclassing plan: subclassing the calculator ties every carrier to the parent's protected internals, and inheritance isn't on the escalation ladder at all. Should land on the mechanics, not just the pattern name: extract an interface named for the question rather than the mechanism (`ShippingCost` / `Carrier`, not `AbstractCarrierCalculatorBase`), one final implementation per carrier, each taking its own required constructor arguments (API keys, base rates) with no optionals or defaults, and dispatch by composition — a composite/registry implementation mapping carrier to implementation, as in the MultipleLoaders shape — rather than by a switch that has merely moved. Should note the switch disappearing is the success signal, and that the 7th carrier then becomes a new class plus one wiring line at the composition root, with no existing file edited. Bonus: carrier as a value object rather than a string; a Money return type; decorators for cross-cutting concerns like caching or retries layered over the same interface. Should NOT propose a Template Method base class with an abstract protected hook as the recommended answer, though noting it as the halfway state that converts in one step is fine."
    },
    {
      "id": 9,
      "prompt": "Setting up a new RetryPolicy object — it just holds maxAttempts and backoffMs. I'm going to do an AbstractRetryPolicyFactory with a concrete DefaultRetryPolicyFactory, plus a RetryPolicyBuilder so callers can do .withMaxAttempts(3).withBackoff(500).build(). There's only one kind of retry policy right now but this way we're ready. Good foundation?",
      "expected_output": "Should decline the indirection directly and give the reason rather than a taste-based objection: a pattern buys variation with indirection, and with only one product and one implementation there is no axis of variation to spend it on — Abstract Factory with a single product family is complexity paid for nothing, and the interface will be shaped by a guess that the real second case probably won't fit. Should separately object to the Builder: a builder is an object that is invalid until build() is called, which is exactly the trap the 'require the minimum data to behave consistently' rule exists to close — RetryPolicy has two required fields, so a plain required constructor plus named constructors (RetryPolicy.of(3, 500), RetryPolicy.noRetries(), RetryPolicy.exponential(...)) with a private constructor gives readable call sites AND a valid object at every moment. Should validate the invariants that are actually missing (maxAttempts >= 1, backoff non-negative) and suggest value objects or at least meaningful types over two bare ints. Should cite 'abstract early, generalize late' — introducing an interface at the first implementation is cheap and fine; writing factories and builders for implementations nobody has asked for is not. Should acknowledge Builder is legitimate for genuinely large composites and in test fixtures, so the pushback isn't dogma. Should NOT simply agree that it's a good foundation, and should NOT propose a third pattern as a compromise."
    },
    {
      "id": 10,
      "prompt": "Standard stuff I think — ConfigManager as a singleton with getInstance() so anything in the app can read config, and it exposes addListener() so services can subscribe to config reloads at runtime. This is straight out of GoF (Singleton + Observer), any objection?",
      "expected_output": "Should engage with GoF rather than pretending the patterns are unknown, and say why this guideline overrides the book on both counts. On Singleton: getInstance() is a service locator with better manners — the dependency never appears in a constructor signature, so a class with four hidden collaborators reads as one with none and no design pressure ever builds; it also welds every client to a lifetime decision and makes tests share mutable state. The key reframe to hit: 'there must be exactly one' is a WIRING fact, not a property of the class — instantiate once at the composition root and inject it, and the class stays ordinary and instantiable. Should also flag injecting a whole ConfigManager as its own problem (inject the specific values a service needs, not a generic config object you call .get() on). On addListener(): registration after construction is a behavior-changing setter, so the service takes different execution paths depending on who called what first — pass listeners to the constructor instead, and if listeners genuinely must be dynamic, that belongs in a dispatcher wired at the composition root rather than on the domain service. Bonus: for reload-at-runtime specifically, note that a service that must be reconfigured mid-life is usually a sign the changing value is task/contextual data belonging on the method, and consider a named notification interface over a generic dispatcher. Should NOT be dismissive of GoF or claim the patterns are simply wrong — the answer is that this skill ranks them, with reasons."
    }
  ]
}
